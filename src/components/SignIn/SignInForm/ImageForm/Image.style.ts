"use client";
import styled from "styled-components";

export const Optional = styled.span`
  position: absolute;
  right: var(--s0);
  top: 22px;
  color: var(--grey-300);
  font-size: var(--font-small);
`;
