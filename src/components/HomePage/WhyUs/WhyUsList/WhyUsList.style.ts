'use client'
import styled from 'styled-components';

export const OList = styled.ol`
    display: flex;
    flex-flow: column;
    
    gap: var(--s2);
`

export const Item = styled.li`
    display: flex;
    flex-flow: column;
    gap: var(--s0);
`